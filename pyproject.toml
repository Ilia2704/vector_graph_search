[project]
name = "vector-graph-search-demo"
version = "0.1.0"
description = "Local Qdrant + graph retrieval demo for RAG lecture"
requires-python = "==3.11.9"
dependencies = [
    "qdrant-client==1.13.3",
    "numpy==2.2.3",
]

[tool.uv]
package = false